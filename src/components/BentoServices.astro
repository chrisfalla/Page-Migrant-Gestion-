---
// Sección de Servicios Especializados - Diseño Bento Grid Profesional
---

<section id="servicios" class="services-section reveal">
  <div class="services-container">
    
    <!-- Header -->
    <div class="services-header">
      <span class="services-badge">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="badge-icon">
          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
        </svg>
        Servicios Especializados
      </span>
      <h2 class="services-title">
        Más de <span class="text-highlight">30 trámites</span> para tu 
        <br class="hidden-mobile" />proceso migratorio
      </h2>
      <p class="services-subtitle">
        Soluciones integrales organizadas para facilitar tu legalización en Colombia
      </p>
    </div>

    <!-- Bento Grid -->
    <div class="services-grid">
      
      <!-- Card 1: Movilidad y Visas (Featured) -->
      <div class="service-card service-card--featured">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <rect x="3" y="4" width="18" height="16" rx="2"/>
                <path d="M3 10h18"/>
                <circle cx="12" cy="15" r="2"/>
              </svg>
            </div>
            <span class="card-badge">Más solicitado</span>
          </div>
          <h3 class="card-title">Movilidad y Visas</h3>
          <p class="card-desc">
            Gestión completa de procesos migratorios, permisos de permanencia y documentación para extranjeros.
          </p>
          <ul class="card-services">
            <li><span class="service-dot"></span>Visas (Todas las categorías)</li>
            <li><span class="service-dot"></span>Permiso Por Protección Temporal (PPT)</li>
            <li><span class="service-dot"></span>Salvoconductos SC-2</li>
            <li><span class="service-dot"></span>Registro SIRE (Empleadores)</li>
            <li><span class="service-dot"></span>Cédula de Extranjería</li>
            <li><span class="service-dot"></span>Renovaciones y cambios</li>
          </ul>
          <a href="#contacto" class="card-cta">
            Consultar disponibilidad
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
        </div>
      </div>
      
      <!-- Card 2: Corporativo y Empleo -->
      <div class="service-card">
        <div class="card-content">
          <div class="card-icon card-icon--blue">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <rect x="2" y="7" width="20" height="14" rx="2"/>
              <path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/>
              <path d="M12 12v4"/>
              <path d="M2 12h20"/>
            </svg>
          </div>
          <h3 class="card-title">Corporativo y Empleo</h3>
          <p class="card-desc">
            Servicios empresariales y gestión legal para empresas que contratan extranjeros.
          </p>
          <ul class="card-services">
            <li><span class="service-dot service-dot--blue"></span>Creación de empresas</li>
            <li><span class="service-dot service-dot--blue"></span>Registro SIRE Empleadores</li>
            <li><span class="service-dot service-dot--blue"></span>Elaboración de hojas de vida</li>
            <li><span class="service-dot service-dot--blue"></span>Registro ante la DIAN</li>
            <li><span class="service-dot service-dot--blue"></span>Consultoría laboral</li>
          </ul>
        </div>
      </div>
      
      <!-- Card 3: Legal y Documentación -->
      <div class="service-card">
        <div class="card-content">
          <div class="card-icon card-icon--purple">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 3v18"/>
              <path d="M5 7h14"/>
              <path d="M7 7l-2 5c0 1.5 1.5 3 4 3s4-1.5 4-3l-2-5"/>
              <path d="M17 7l-2 5c0 1.5 1.5 3 4 3s4-1.5 4-3l-2-5"/>
            </svg>
          </div>
          <h3 class="card-title">Legal y Documentación</h3>
          <p class="card-desc">
            Trámites notariales, apostillas y gestión de antecedentes judiciales.
          </p>
          <ul class="card-services">
            <li><span class="service-dot service-dot--purple"></span>Apostillas internacionales</li>
            <li><span class="service-dot service-dot--purple"></span>Antecedentes judiciales</li>
            <li><span class="service-dot service-dot--purple"></span>Trámites de notaría</li>
            <li><span class="service-dot service-dot--purple"></span>Tutelas y derechos de petición</li>
            <li><span class="service-dot service-dot--purple"></span>Asesoría jurídica</li>
          </ul>
        </div>
      </div>
      
      <!-- Card 4: Educación e Integración (Featured) -->
      <div class="service-card service-card--featured-alt">
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon card-icon--green">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M22 10v6M2 10l10-5 10 5-10 5z"/>
                <path d="M6 12v5c0 2 2 3 6 3s6-1 6-3v-5"/>
              </svg>
            </div>
            <span class="card-badge card-badge--green">Nuevo</span>
          </div>
          <h3 class="card-title">Educación e Integración</h3>
          <p class="card-desc">
            Convalidación de títulos, homologación académica y procesos educativos para migrantes.
          </p>
          <ul class="card-services">
            <li><span class="service-dot service-dot--green"></span>Convalidación de títulos</li>
            <li><span class="service-dot service-dot--green"></span>Homologación académica</li>
            <li><span class="service-dot service-dot--green"></span>Bachiller por validación</li>
            <li><span class="service-dot service-dot--green"></span>Registro RUMV (Migrantes Vzla)</li>
            <li><span class="service-dot service-dot--green"></span>Cursos de integración</li>
            <li><span class="service-dot service-dot--green"></span>Orientación educativa</li>
          </ul>
        </div>
      </div>
      
      <!-- Card 5: Trámites Especiales -->
      <div class="service-card service-card--special">
        <div class="card-content">
          <div class="card-icon card-icon--amber">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 2l2.4 7.4H22l-6 4.6 2.3 7L12 17l-6.3 4 2.3-7-6-4.6h7.6z"/>
            </svg>
          </div>
          <h3 class="card-title">Trámites Especiales</h3>
          <p class="card-desc">
            Servicios adicionales para necesidades específicas de la comunidad migrante.
          </p>
          <ul class="card-services">
            <li><span class="service-dot service-dot--amber"></span>Tarjeta de Movilidad Fronteriza</li>
            <li><span class="service-dot service-dot--amber"></span>Permiso de Ingreso y Permanencia</li>
            <li><span class="service-dot service-dot--amber"></span>Certificados y constancias</li>
            <li><span class="service-dot service-dot--amber"></span>Asesoría integral</li>
          </ul>
        </div>
      </div>
      
    </div>

    <!-- CTA Section -->
    <div class="services-cta">
      <div class="cta-content">
        <p class="cta-question">¿No encuentras el trámite que necesitas?</p>
        <p class="cta-answer">Contamos con más de 30 servicios adicionales. Contáctanos y te asesoramos.</p>
      </div>
      <a href="#contacto" class="cta-button">
        <span>Consultar ahora</span>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

  </div>
</section>

<style>
  .services-section {
    padding: var(--space-8) var(--space-4);
    background: linear-gradient(180deg, var(--color-gray-50) 0%, var(--color-white) 100%);
    position: relative;
    overflow: hidden;
  }

  @media (min-width: 640px) {
    .services-section { padding-inline: var(--space-6); }
  }
  @media (min-width: 1024px) {
    .services-section { padding-inline: var(--space-8); }
  }

  .services-container {
    max-width: var(--container-xl);
    margin-inline: auto;
  }

  /* Header */
  .services-header {
    text-align: center;
    margin-bottom: var(--space-6);
  }

  .services-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-4);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-icon {
    width: 16px;
    height: 16px;
  }

  .services-title {
    font-family: var(--font-display);
    font-size: clamp(var(--text-3xl), 4vw, var(--text-5xl));
    font-weight: var(--font-extrabold);
    color: var(--color-black);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-4);
  }

  .text-highlight {
    color: var(--color-primary);
    position: relative;
  }

  .text-highlight::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 8px;
    background: var(--color-primary);
    opacity: 0.15;
    border-radius: 4px;
  }

  .hidden-mobile {
    display: none;
  }

  @media (min-width: 768px) {
    .hidden-mobile { display: block; }
  }

  .services-subtitle {
    font-size: var(--text-lg);
    color: var(--color-gray-600);
    max-width: 50ch;
    margin-inline: auto;
    line-height: var(--leading-relaxed);
  }

  /* Bento Grid */
  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  @media (min-width: 768px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(3, 1fr);
      grid-auto-rows: min-content;
    }
  }

  /* Service Cards */
  .service-card {
    position: relative;
    background: var(--color-white);
    border-radius: var(--radius-2xl);
    border: 1px solid var(--color-gray-200);
    overflow: hidden;
    transition: all var(--transition-base) var(--ease-out);
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-xl);
    border-color: transparent;
  }

  .card-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-accent) 100%);
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .service-card:hover .card-glow {
    opacity: 1;
  }

  .card-content {
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 768px) {
    .card-content {
      padding: var(--space-6);
    }
  }

  .card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-3);
  }

  /* Card Icons */
  .card-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-lg);
    background: var(--color-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-3);
    transition: transform var(--transition-base);
  }

  .service-card:hover .card-icon {
    transform: scale(1.05);
  }

  .card-icon svg {
    width: 24px;
    height: 24px;
    stroke: var(--color-white);
  }

  .card-icon--blue {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  }

  .card-icon--purple {
    background: linear-gradient(135deg, #8b5cf6 0%, #6d28d9 100%);
  }

  .card-icon--green {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  }

  .card-icon--amber {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
  }

  /* Card Badge */
  .card-badge {
    padding: var(--space-1) var(--space-3);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-full);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .card-badge--green {
    background: #10b981;
  }

  /* Card Content */
  .card-title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: var(--color-black);
    margin-bottom: var(--space-2);
  }

  @media (min-width: 768px) {
    .card-title {
      font-size: var(--text-2xl);
    }
  }

  .card-desc {
    font-size: var(--text-sm);
    color: var(--color-gray-600);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-3);
  }

  /* Services List */
  .card-services {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .card-services li {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-gray-700);
  }

  .service-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-primary);
    flex-shrink: 0;
  }

  .service-dot--blue { background: #3b82f6; }
  .service-dot--purple { background: #8b5cf6; }
  .service-dot--green { background: #10b981; }
  .service-dot--amber { background: #f59e0b; }

  /* Card CTA */
  .card-cta {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    margin-top: var(--space-4);
    padding: var(--space-2) var(--space-4);
    background: var(--color-gray-100);
    color: var(--color-primary);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition: all var(--transition-base);
  }

  .card-cta:hover {
    background: var(--color-primary);
    color: var(--color-white);
    gap: var(--space-3);
  }

  .card-cta svg {
    width: 18px;
    height: 18px;
  }

  /* Featured Card Styles */
  .service-card--featured {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
    border: none;
  }

  .service-card--featured .card-icon {
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(8px);
  }

  .service-card--featured .card-title,
  .service-card--featured .card-desc,
  .service-card--featured .card-services li {
    color: var(--color-white);
  }

  .service-card--featured .card-desc {
    color: rgba(255, 255, 255, 0.8);
  }

  .service-card--featured .service-dot {
    background: var(--color-white);
  }

  .service-card--featured .card-cta {
    background: var(--color-white);
    color: var(--color-primary);
  }

  .service-card--featured .card-cta:hover {
    background: var(--color-gray-100);
  }

  .service-card--featured:hover {
    transform: translateY(-4px);
    box-shadow: 0 25px 50px -12px rgba(0, 27, 72, 0.4);
  }

  /* Featured Alt Card */
  .service-card--featured-alt {
    background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
    border-color: #86efac;
  }

  .service-card--featured-alt:hover {
    border-color: #22c55e;
  }

  /* Special Card */
  .service-card--special {
    background: linear-gradient(135deg, #fffbeb 0%, #fef3c7 100%);
    border-color: #fcd34d;
  }

  .service-card--special:hover {
    border-color: #f59e0b;
  }

  /* CTA Section */
  .services-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-5);
    background: var(--color-white);
    border-radius: var(--radius-xl);
    border: 2px dashed var(--color-gray-300);
    text-align: center;
  }

  @media (min-width: 768px) {
    .services-cta {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
    }
  }

  .cta-content {
    flex: 1;
  }

  .cta-question {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--color-black);
    margin-bottom: var(--space-1);
  }

  .cta-answer {
    font-size: var(--text-sm);
    color: var(--color-gray-600);
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-4) var(--space-6);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-xl);
    text-decoration: none;
    transition: all var(--transition-base);
    white-space: nowrap;
  }

  .cta-button:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    gap: var(--space-3);
  }

  .cta-button svg {
    width: 20px;
    height: 20px;
  }
</style>
