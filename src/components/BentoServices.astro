---
// Sección de Servicios Especializados — Diseño Profesional Navy Blue
// 4 categorías macro | Cards blancas | Acentos sutiles
---

<section id="servicios" class="services-section reveal">
  <div class="services-container">
    
    <!-- Header -->
    <div class="services-header">
      <span class="services-badge">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="badge-icon">
          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
        </svg>
        Servicios Especializados
      </span>
      <h2 class="services-title">
        Más de <span class="text-highlight">40 trámites</span> para tu 
        <br class="hidden-mobile" />proceso migratorio
      </h2>
      <p class="services-subtitle">
        Soluciones integrales organizadas para facilitar tu legalización en Colombia
      </p>
    </div>

    <!-- Grid de 4 Categorías -->
    <div class="services-grid">
      
      <!-- Card 1: Personas y Familias -->
      <div class="service-card">
        <div class="card-accent"></div>
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <rect x="3" y="4" width="18" height="16" rx="2"/>
                <path d="M3 10h18"/>
                <circle cx="12" cy="15" r="2"/>
              </svg>
            </div>
            <span class="card-badge">Más solicitado</span>
          </div>
          <h3 class="card-title">Personas y Familias</h3>
          <p class="card-desc">
            Gestión completa de visas, permisos de permanencia, salud, educación y documentación para extranjeros y sus familias.
          </p>
          <ul class="card-services">
            <li><span class="service-dot"></span>Análisis de visas colombianas aplicables</li>
            <li><span class="service-dot"></span>Elaboración documentación para visas</li>
            <li><span class="service-dot"></span>Permiso Por Protección Temporal (PPT)</li>
            <li class="service-hidden"><span class="service-dot"></span>Salvoconductos, cédula de extranjería</li>
            <li class="service-hidden"><span class="service-dot"></span>Afiliación Sistema de Salud</li>
            <li class="service-hidden"><span class="service-dot"></span>Convalidación títulos Min. Educación</li>
            <li class="service-hidden"><span class="service-dot"></span>Validación bachiller y licencias profesionales</li>
            <li class="service-hidden"><span class="service-dot"></span>Representación ante autoridades migratorias</li>
          </ul>
          <button class="btn-ver-mas" aria-label="Ver más servicios">
            <span class="ver-mas-text">Ver más servicios</span>
            <svg class="ver-mas-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"/>
            </svg>
          </button>
          <div class="card-footer">
            <span class="card-count">+15 trámites</span>
            <a href="#contacto" class="card-cta">
              Consultar
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
      
      <!-- Card 2: Empresas y Corporativo -->
      <div class="service-card">
        <div class="card-accent"></div>
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <rect x="2" y="7" width="20" height="14" rx="2"/>
                <path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/>
                <path d="M12 12v4"/>
                <path d="M2 12h20"/>
              </svg>
            </div>
          </div>
          <h3 class="card-title">Empresas y Corporativo</h3>
          <p class="card-desc">
            Servicios empresariales, gestión legal, trámites tributarios y capacitaciones para empresas que contratan extranjeros.
          </p>
          <ul class="card-services">
            <li><span class="service-dot"></span>Creación de empresas plataforma SIRE</li>
            <li><span class="service-dot"></span>Capacitaciones contratación extranjeros</li>
            <li><span class="service-dot"></span>Citas DIAN y trámites tributarios</li>
            <li class="service-hidden"><span class="service-dot"></span>Diligenciamiento FUT virtual</li>
            <li class="service-hidden"><span class="service-dot"></span>Legalización y apostilla documentos</li>
            <li class="service-hidden"><span class="service-dot"></span>Tutelas y derechos de petición</li>
            <li class="service-hidden"><span class="service-dot"></span>Asesoría laboral integral</li>
          </ul>
          <button class="btn-ver-mas" aria-label="Ver más servicios">
            <span class="ver-mas-text">Ver más servicios</span>
            <svg class="ver-mas-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"/>
            </svg>
          </button>
          <div class="card-footer">
            <span class="card-count">+10 trámites</span>
            <a href="#contacto" class="card-cta">
              Consultar
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
      
      <!-- Card 3: Servicios para Venezolanos -->
      <div class="service-card">
        <div class="card-accent"></div>
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <circle cx="12" cy="12" r="10"/>
                <path d="M12 6v6l4 2"/>
              </svg>
            </div>
            <span class="card-badge card-badge--subtle">Venezuela</span>
          </div>
          <h3 class="card-title">Servicios para Venezolanos</h3>
          <p class="card-desc">
            Trámites especializados ante instituciones venezolanas para ciudadanos que requieren documentación oficial.
          </p>
          <ul class="card-services">
            <li><span class="service-dot"></span>Agendamiento citas pasaporte VEN</li>
            <li><span class="service-dot"></span>Creación usuarios SAIME – MPRE – SAREN</li>
            <li><span class="service-dot"></span>Solución error migratorio SAIME</li>
            <li class="service-hidden"><span class="service-dot"></span>Antecedentes penales venezolanos</li>
            <li class="service-hidden"><span class="service-dot"></span>Apostilla documentos venezolanos</li>
            <li class="service-hidden"><span class="service-dot"></span>Regularización niños venezolanos (RUMV)</li>
          </ul>
          <button class="btn-ver-mas" aria-label="Ver más servicios">
            <span class="ver-mas-text">Ver más servicios</span>
            <svg class="ver-mas-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"/>
            </svg>
          </button>
          <div class="card-footer">
            <span class="card-count">+8 trámites</span>
            <a href="#contacto" class="card-cta">
              Consultar
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
      
      <!-- Card 4: Trámites Colombianos -->
      <div class="service-card">
        <div class="card-accent"></div>
        <div class="card-content">
          <div class="card-header">
            <div class="card-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 2l2.4 7.4H22l-6 4.6 2.3 7L12 17l-6.3 4 2.3-7-6-4.6h7.6z"/>
              </svg>
            </div>
          </div>
          <h3 class="card-title">Trámites Colombianos</h3>
          <p class="card-desc">
            Servicios de acompañamiento para trámites ante entidades colombianas, certificados y documentación oficial.
          </p>
          <ul class="card-services">
            <li><span class="service-dot"></span>Agendamiento citas pasaporte COL</li>
            <li><span class="service-dot"></span>Inscripción extemporánea hijos exterior</li>
            <li><span class="service-dot"></span>Certificados SNMC, Policía, Procuraduría</li>
            <li class="service-hidden"><span class="service-dot"></span>Certificados Contraloría</li>
            <li class="service-hidden"><span class="service-dot"></span>Antecedentes penales colombianos</li>
            <li class="service-hidden"><span class="service-dot"></span>Apostilla documentos colombianos</li>
          </ul>
          <button class="btn-ver-mas" aria-label="Ver más servicios">
            <span class="ver-mas-text">Ver más servicios</span>
            <svg class="ver-mas-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M19 9l-7 7-7-7"/>
            </svg>
          </button>
          <div class="card-footer">
            <span class="card-count">+7 trámites</span>
            <a href="#contacto" class="card-cta">
              Consultar
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
            </a>
          </div>
        </div>
      </div>
      
    </div>

    <!-- CTA Section -->
    <div class="services-cta">
      <div class="cta-content">
        <p class="cta-question">¿Tienes alguna duda sobre nuestros servicios?</p>
        <p class="cta-answer">Contamos con más de 40 servicios especializados. Contáctanos para una asesoría personalizada.</p>
      </div>
      <a href="#contacto" class="cta-button">
        <span>Consultar ahora</span>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
    </div>

  </div>
</section>

<style>
  /* ─── Sección ─── */
  .services-section {
    padding: var(--space-16) var(--space-4);
    background: var(--color-gray-50);
    position: relative;
    overflow: hidden;
  }

  @media (min-width: 640px) {
    .services-section { padding-inline: var(--space-6); }
  }
  @media (min-width: 1024px) {
    .services-section { padding-inline: var(--space-8); }
  }

  .services-container {
    max-width: var(--container-xl);
    margin-inline: auto;
  }

  /* ─── Header ─── */
  .services-header {
    text-align: center;
    margin-bottom: var(--space-10);
  }

  .services-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-4);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-icon {
    width: 16px;
    height: 16px;
  }

  .services-title {
    font-family: var(--font-display);
    font-size: clamp(var(--text-3xl), 4vw, var(--text-5xl));
    font-weight: var(--font-extrabold);
    color: var(--color-primary);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-4);
  }

  .text-highlight {
    color: var(--color-accent);
    position: relative;
  }

  .hidden-mobile {
    display: none;
  }

  @media (min-width: 768px) {
    .hidden-mobile { display: block; }
  }

  .services-subtitle {
    font-size: var(--text-lg);
    color: var(--color-gray-600);
    max-width: 50ch;
    margin-inline: auto;
    line-height: var(--leading-relaxed);
  }

  /* ─── Grid: 1 col mobile → 2 col tablet → 4 col desktop ─── */
  .services-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    margin-bottom: var(--space-10);
  }

  @media (min-width: 768px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .services-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* ─── Cards ─── */
  .service-card {
    position: relative;
    background: var(--color-white);
    border-radius: var(--radius-2xl);
    border: 1px solid var(--color-gray-200);
    box-shadow: var(--shadow-card);
    overflow: hidden;
    transition: all 0.5s var(--ease-in-out);
    display: flex;
    flex-direction: column;
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-card-hover);
    border-color: var(--color-accent);
  }

  /* Línea de acento superior — sutil, solo Navy → Accent */
  .card-accent {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-accent) 100%);
    opacity: 0;
    transition: opacity 0.5s var(--ease-in-out);
  }

  .service-card:hover .card-accent {
    opacity: 1;
  }

  .card-content {
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  @media (min-width: 768px) {
    .card-content {
      padding: var(--space-6) var(--space-5);
    }
  }

  .card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-3);
  }

  /* ─── Iconos — Navy Blue uniforme ─── */
  .card-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-lg);
    background: var(--color-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: transform 0.5s var(--ease-in-out), background 0.5s var(--ease-in-out);
  }

  .service-card:hover .card-icon {
    transform: scale(1.08);
    background: var(--color-accent);
  }

  .card-icon svg {
    width: 24px;
    height: 24px;
    stroke: var(--color-white);
  }

  /* ─── Badge ─── */
  .card-badge {
    padding: var(--space-1) var(--space-3);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-full);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .card-badge--subtle {
    background: var(--color-gray-100);
    color: var(--color-primary);
    border: 1px solid var(--color-gray-300);
  }

  /* ─── Contenido ─── */
  .card-title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: var(--color-primary);
    margin-bottom: var(--space-2);
  }

  @media (min-width: 768px) {
    .card-title {
      font-size: var(--text-2xl);
    }
  }

  .card-desc {
    font-size: var(--text-sm);
    color: var(--color-gray-600);
    line-height: var(--leading-relaxed);
    margin-bottom: var(--space-4);
  }

  /* ─── Lista de servicios ─── */
  .card-services {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
    flex: 1;
  }

  .card-services li {
    display: flex;
    align-items: flex-start;
    gap: var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-gray-700);
    line-height: var(--leading-snug);
  }

  .service-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-accent);
    flex-shrink: 0;
    margin-top: 6px;
  }

  /* ─── Footer de card ─── */
  .card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: var(--space-5);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-gray-100);
  }

  .card-count {
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    color: var(--color-gray-400);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .card-cta {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: transparent;
    color: var(--color-primary);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition: all 0.4s var(--ease-in-out);
    border: 1px solid var(--color-gray-200);
  }

  .card-cta:hover {
    background: var(--color-primary);
    color: var(--color-white);
    border-color: var(--color-primary);
    gap: var(--space-3);
  }

  .card-cta svg {
    width: 16px;
    height: 16px;
  }

  /* ─── CTA Section ─── */
  .services-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-6);
    background: var(--color-white);
    border-radius: var(--radius-2xl);
    border: 1px solid var(--color-gray-200);
    box-shadow: var(--shadow-card);
    text-align: center;
  }

  @media (min-width: 768px) {
    .services-cta {
      flex-direction: row;
      justify-content: space-between;
      text-align: left;
      padding: var(--space-6) var(--space-8);
    }
  }

  .cta-content {
    flex: 1;
  }

  .cta-question {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--color-primary);
    margin-bottom: var(--space-1);
  }

  .cta-answer {
    font-size: var(--text-sm);
    color: var(--color-gray-600);
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-4) var(--space-6);
    background: var(--color-primary);
    color: var(--color-white);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    border-radius: var(--radius-xl);
    text-decoration: none;
    transition: all 0.5s var(--ease-in-out);
    white-space: nowrap;
  }

  .cta-button:hover {
    background: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: var(--shadow-primary);
    gap: var(--space-3);
  }

  .cta-button svg {
    width: 20px;
    height: 20px;
  }
</style>

<script>
  // Funcionalidad Ver más/Ver menos
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('.btn-ver-mas');
    
    buttons.forEach(button => {
      button.addEventListener('click', () => {
        const card = button.closest('.service-card');
        const textSpan = button.querySelector('.ver-mas-text');
        
        if (card && textSpan) {
          card.classList.toggle('expanded');
          
          if (card.classList.contains('expanded')) {
            textSpan.textContent = 'Ver menos';
          } else {
            textSpan.textContent = 'Ver más servicios';
          }
        }
      });
    });
  });
</script>
