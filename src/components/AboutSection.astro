---
import { Image } from 'astro:assets';
import teamImage from '../assets/img/business-people-shaking-hands-together.jpg';
---

<section id="nosotros" class="about-section reveal">
  <div class="about-container">
    
    <!-- Header de sección -->
    <div class="about-header">
      <span class="about-badge">Nuestra Historia</span>
      <h2 class="about-title">
        Más de <span class="text-highlight">5 años</span> transformando
        <br />vidas de migrantes
      </h2>
      <p class="about-lead">
        Conoce quiénes somos, por qué hacemos lo que hacemos, y cómo hemos ayudado 
        a cientos de personas a legalizar su situación migratoria en Colombia.
      </p>
    </div>

    <!-- Grid principal -->
    <div class="about-grid">
      
      <!-- Columna izquierda: Historia -->
      <div class="about-story">
        <div class="story-card">
          <div class="story-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 8v4l3 3m6-3a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
            </svg>
          </div>
          <h3 class="story-title">¿Cómo comenzó todo?</h3>
          <div class="story-content">
            <p>
              <strong>MIGRANT GESTIÓN SAS</strong> nació en <strong>2020</strong> en Neiva, Huila, 
              en medio de una de las mayores crisis migratorias de la región. Vimos cómo miles de 
              personas llegaban a Colombia buscando un futuro mejor, pero se encontraban perdidos 
              ante la complejidad de los trámites migratorios.
            </p>
            <p>
              Fundada por <strong>Alexis Jose Cedeño</strong>, nuestra empresa surgió de una 
              motivación personal: haber vivido en carne propia los desafíos de ser migrante 
              y entender que muchas personas necesitaban una mano experta que los guiara.
            </p>
          </div>
        </div>

        <div class="story-card">
          <div class="story-icon story-icon--accent">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </div>
          <h3 class="story-title">Nuestra motivación</h3>
          <div class="story-content">
            <p>
              Creemos firmemente que <strong>migrar no debería ser un obstáculo, sino una oportunidad</strong>. 
              Cada persona que llega a Colombia con sueños merece tener acceso a un proceso 
              migratorio justo, transparente y sin complicaciones innecesarias.
            </p>
            <p>
              Nos motiva ver cómo nuestros clientes logran establecerse legalmente, 
              abrir sus negocios, reunirse con sus familias y construir una nueva vida 
              con tranquilidad y seguridad jurídica.
            </p>
          </div>
        </div>
      </div>

      <!-- Columna derecha: Imagen + Timeline -->
      <div class="about-visual">
        <div class="about-image-wrapper">
          <Image 
            src={teamImage} 
            alt="Equipo de Migrant Gestión brindando asesoría" 
            class="about-image"
            loading="lazy"
            decoding="async"
            width={650}
            height={430}
            quality={65}
            format="webp"
          />
          <div class="image-overlay"></div>
          <div class="image-badge">
            <span class="badge-number">05+</span>
            <span class="badge-text">Años de<br/>experiencia</span>
          </div>
        </div>

        <!-- Timeline -->
        <div class="timeline">
          <div class="timeline-item">
            <div class="timeline-year">2020</div>
            <div class="timeline-content">
              <strong>Fundación</strong> — Comenzamos operaciones en Neiva, Huila
            </div>
          </div>
          <div class="timeline-item">
            <div class="timeline-year">2021</div>
            <div class="timeline-content">
              <strong>Expansión</strong> — Ampliamos servicios a todo el Huila
            </div>
          </div>
          <div class="timeline-item">
            <div class="timeline-year">2023</div>
            <div class="timeline-content">
              <strong>Alcance Nacional</strong> — Atención remota para toda Colombia
            </div>
          </div>
          <div class="timeline-item timeline-item--current">
            <div class="timeline-year">2026</div>
            <div class="timeline-content">
              <strong>Hoy</strong> — +30 tipos de trámites y cientos de casos exitosos
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    padding: clamp(var(--space-10), 5vw, var(--space-16)) var(--space-4);
    background: var(--color-gray-50);
  }

  @media (min-width: 640px) {
    .about-section { padding-inline: var(--space-6); }
  }
  @media (min-width: 1024px) {
    .about-section { padding-inline: var(--space-8); }
  }

  .about-container {
    max-width: var(--container-xl);
    margin-inline: auto;
  }

  .about-header {
    text-align: center;
    max-width: 720px;
    margin: 0 auto var(--space-12);
  }

  .about-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-2) var(--space-4);
    background: var(--color-primary);
    color: var(--color-white);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-4);
  }

  .about-title {
    font-size: clamp(var(--text-2xl), 4vw, var(--text-4xl));
    font-weight: var(--font-extrabold);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-4);
  }

  .text-highlight {
    color: var(--color-primary);
    position: relative;
  }

  .text-highlight::after {
    content: '';
    position: absolute;
    bottom: 0.1em;
    left: 0;
    right: 0;
    height: 0.35em;
    background: rgba(0, 27, 72, 0.1);
    border-radius: var(--radius-sm);
    z-index: -1;
  }

  .about-lead {
    font-size: var(--text-lg);
    color: var(--color-gray-500);
    line-height: var(--leading-relaxed);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-8);
    align-items: start;
  }

  @media (min-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-10);
    }
  }

  .about-story {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }

  .story-card {
    background: var(--color-white);
    border-radius: var(--radius-2xl);
    padding: var(--space-6);
    border: 1px solid rgba(0, 0, 0, 0.06);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }

  .story-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.1);
  }

  .story-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-lg);
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    display: grid;
    place-items: center;
    margin-bottom: var(--space-4);
  }

  .story-icon svg {
    width: 24px;
    height: 24px;
    color: var(--color-white);
  }

  .story-icon--accent {
    background: linear-gradient(135deg, var(--color-success) 0%, #059669 100%);
  }

  .story-title {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-3);
  }

  .story-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .story-content p {
    font-size: var(--text-base);
    color: var(--color-gray-600);
    line-height: var(--leading-relaxed);
  }

  .story-content strong {
    color: var(--color-primary);
    font-weight: var(--font-semibold);
  }

  .about-visual {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  @media (max-width: 899px) {
    .about-visual { order: -1; }
  }

  .about-image-wrapper {
    position: relative;
    border-radius: var(--radius-3xl);
    overflow: hidden;
    aspect-ratio: 4 / 3;
  }

  .about-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.2);
  }

  .image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 50%, rgba(0, 27, 72, 0.7) 100%);
  }

  .image-badge {
    position: absolute;
    bottom: var(--space-5);
    left: var(--space-5);
    display: flex;
    align-items: center;
    gap: var(--space-3);
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    padding: var(--space-3) var(--space-5);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
  }

  .badge-number {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: var(--font-extrabold);
    color: var(--color-primary);
    line-height: var(--leading-none);
  }

  .badge-text {
    font-size: var(--text-xs);
    color: var(--color-gray-500);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    line-height: 1.3;
  }

  .timeline {
    display: flex;
    flex-direction: column;
    position: relative;
    padding-left: var(--space-4);
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 0;
    top: var(--space-2);
    bottom: var(--space-2);
    width: 2px;
    background: linear-gradient(180deg, var(--color-primary) 0%, rgba(0, 27, 72, 0.2) 100%);
    border-radius: var(--radius-full);
  }

  .timeline-item {
    display: grid;
    grid-template-columns: 56px 1fr;
    gap: var(--space-4);
    padding: var(--space-3) 0;
    position: relative;
  }

  .timeline-item::before {
    content: '';
    position: absolute;
    left: calc(-1 * var(--space-4));
    top: calc(var(--space-3) + 6px);
    width: 10px;
    height: 10px;
    background: var(--color-primary);
    border-radius: var(--radius-full);
    transform: translateX(-50%);
    border: 2px solid var(--color-white);
    box-shadow: 0 0 0 3px rgba(0, 27, 72, 0.15);
  }

  .timeline-item--current::before {
    background: var(--color-success);
    box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.2);
  }

  .timeline-year {
    font-size: var(--text-sm);
    font-weight: var(--font-bold);
    color: var(--color-primary);
  }

  .timeline-content {
    font-size: var(--text-sm);
    color: var(--color-gray-600);
    line-height: var(--leading-normal);
  }

  .timeline-content strong {
    color: var(--color-black);
    font-weight: var(--font-semibold);
  }

  @media (max-width: 640px) {
    .about-header { margin-bottom: var(--space-8); }
    .story-card { padding: var(--space-5); }
    .timeline-item { grid-template-columns: 48px 1fr; }
  }
</style>
